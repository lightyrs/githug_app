.session
  - if user_signed_in?
    .current-user Signed in as #{link_to current_user.email, user_path(current_user)}.
    .action
      Not you?
      = link_to "Sign out", destroy_user_session_path
  - else
    = link_to "Sign in with github", user_omniauth_authorize_path(:github)
